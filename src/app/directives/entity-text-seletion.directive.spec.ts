import { EntityTextSeletionDirective } from './entity-text-seletion.directive';

describe('EntityTextSeletionDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityTextSeletionDirective();
    expect(directive).toBeTruthy();
  });
});
